import javax.swing.*;

public class Componentes_GUI {

    public static void main(String[] args)
    {
        if(false) {
            LabelFrame labelFrame = new LabelFrame();
            labelFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            labelFrame.setSize(260, 180);
            labelFrame.setVisible(true);
        }

        if(false) {
            TextFieldFrame textFieldFrame= new TextFieldFrame();
            textFieldFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            textFieldFrame.setSize(260, 180);
            textFieldFrame.setVisible(true);
        }

        if(false) {
            ButtonFrame buttonFrame = new ButtonFrame();
            buttonFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            buttonFrame.setSize(260, 180);
            buttonFrame.setVisible(true);
        }

        if(false) {
            CheckBoxFrame checkBoxFrame = new CheckBoxFrame();
            checkBoxFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            checkBoxFrame.setSize(260, 180);
            checkBoxFrame.setVisible(true);
        }

        if(false) {
            RadioButtonFrame radioButtonFrame = new RadioButtonFrame();
            radioButtonFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            radioButtonFrame.setSize(260, 180);
            radioButtonFrame.setVisible(true);
        }

        if(false) {
            ComboBoxFrame comboBoxFrame = new ComboBoxFrame();
            comboBoxFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            comboBoxFrame.setSize(260, 180);
            comboBoxFrame.setVisible(true);
        }

        if(false) {
            ListFrame listFrame = new ListFrame();
            listFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            listFrame.setSize(260, 180);
            listFrame.setVisible(true);
        }

        if(false) {
            MouseTrackerFrame mouseTrackerFrame = new MouseTrackerFrame();
            mouseTrackerFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            mouseTrackerFrame.setSize(260, 180);
            mouseTrackerFrame.setVisible(true);
        }


    }

}
